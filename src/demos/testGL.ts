import { RendererGL,ObjectGL, Scene, Cube, SkyBox, AxesHelper, Fly1Control, OrbitControl, Mat4, Vec3, Camera } from "@/engine";

class Test extends ObjectGL {

    constructor(){
        super({
            
        })
    }
}